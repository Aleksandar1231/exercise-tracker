# exercise-tracker

To run on local machine, open 2 command shells:

  On first command shell, start the app:
    $npm start
  
  On second command shell, go into backend directory and start the server:
    $cd backend
    $nodemon server
  
